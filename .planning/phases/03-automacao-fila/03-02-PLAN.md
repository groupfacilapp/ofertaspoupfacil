---
phase: 03-automacao-fila
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(dashboard)/page.tsx
autonomous: true
requirements: [DASH-01, DASH-02, DASH-03, DASH-04]
must_haves:
  truths:
    - "Dashboard shows 7-day bar chart of dispatches using CSS/SVG bars (no recharts)"
    - "Dashboard shows stats cards: Total Produtos, Enviados hoje, Pendentes, Sucesso, Falhas"
    - "Dashboard shows products per marketplace with horizontal progress bars"
    - "Dashboard shows WhatsApp instance connection status card"
  artifacts:
    - path: "src/app/(dashboard)/page.tsx"
      provides: "Enhanced dashboard with charts, stats, marketplace bars, WA status"
      min_lines: 150
  key_links:
    - from: "src/app/(dashboard)/page.tsx"
      to: "dispatch_logs table"
      via: "supabaseAdmin queries for 7-day aggregation"
      pattern: "dispatched_date"
    - from: "src/app/(dashboard)/page.tsx"
      to: "offers table"
      via: "supabaseAdmin count queries"
      pattern: "from\\('offers'\\)"
    - from: "src/app/(dashboard)/page.tsx"
      to: "whatsapp_instances table"
      via: "supabaseAdmin select status"
      pattern: "whatsapp_instances"
---

<objective>
Rebuild the dashboard page with rich stats: 7-day dispatch bar chart (pure CSS bars), per-marketplace product counts with progress bars, general stats cards, and WhatsApp connection status.

Purpose: Users see a comprehensive overview of their operation -- dispatches over time, product distribution, and system health.
Output: Fully rewritten dashboard page.tsx.
</objective>

<execution_context>
@/Users/renangalhardo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/renangalhardo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md

<interfaces>
From src/app/(dashboard)/page.tsx (current):
- Server component, uses `createClient()` for auth, `supabaseAdmin` for data
- Dark theme: bg-zinc-950, bg-zinc-900/60, border-zinc-800/60, text-white, text-zinc-400
- Stats cards pattern: rounded-xl border border-zinc-800/60 bg-zinc-900/60 p-5
- Icons from lucide-react
- Greeting with BRT timezone offset

From src/lib/supabase/admin.ts:
```typescript
export const supabaseAdmin = createClient(...);
```

Existing tables available:
- `offers` (user_id, marketplace, fetched_at) -- product catalog
- `dispatch_logs` (user_id, status, dispatched_date, dispatched_at, offer_id) -- dispatch history
- `whatsapp_instances` (user_id, status) -- WA connection status
- `marketplace_connections` (user_id, marketplace, is_valid) -- marketplace connections
- `dispatch_groups` (user_id, is_active) -- groups
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite dashboard with 7-day chart, rich stats, marketplace bars, and WA status</name>
  <files>src/app/(dashboard)/page.tsx</files>
  <read_first>
    src/app/(dashboard)/page.tsx,
    src/app/(dashboard)/historico/page.tsx,
    src/lib/supabase/admin.ts
  </read_first>
  <action>
Rewrite `src/app/(dashboard)/page.tsx` as a server component. Keep the existing greeting logic and auth flow. Replace the stats/steps sections with:

**Data fetching (all via supabaseAdmin, in parallel with Promise.all):**

1. **7-day dispatch data**: Query `dispatch_logs` for last 7 days grouped by `dispatched_date`. Build array of `{ date: string, label: string, count: number }` for each of last 7 days (including days with 0). Use SQL-style: fetch all logs where `dispatched_date >= 7 days ago`, then bucket in JS.

```typescript
const sevenDaysAgo = new Date();
sevenDaysAgo.setDate(sevenDaysAgo.getDate() - 6);
const sevenDaysAgoStr = sevenDaysAgo.toISOString().split('T')[0];

const { data: weekLogs } = await supabaseAdmin
  .from('dispatch_logs')
  .select('dispatched_date, status')
  .eq('user_id', user.id)
  .gte('dispatched_date', sevenDaysAgoStr)
  .in('status', ['sent', 'delivered', 'read']);
```

Then build a map of date -> count, fill in 0 for missing days. Labels should be weekday abbreviations in PT-BR: "Seg", "Ter", "Qua", "Qui", "Sex", "Sab", "Dom".

2. **General stats**: Query counts from offers (total), dispatch_logs today (sent+delivered+read = success, failed, pending), offers without dispatch_log today = pending products.

3. **Products per marketplace**: Query `offers` grouped by marketplace. Use `supabaseAdmin.from('offers').select('marketplace').eq('user_id', user.id)` then count in JS.

4. **WhatsApp status**: Query `whatsapp_instances` for user, get status field.

5. **Keep existing queries** for connectedMarketplaces, activeGroups.

**Rendering (dark theme, matching existing design system):**

**Section 1: Greeting** (keep existing)

**Section 2: Stats cards row** (grid-cols-2 sm:grid-cols-3 lg:grid-cols-5 gap-3):
- "Total Produtos" - indigo icon (Package), count from offers
- "Enviados hoje" - emerald icon (Send), count of today's sent dispatch_logs
- "Pendentes" - amber icon (Clock), offers without dispatch_log today
- "Sucesso" - emerald icon (CheckCircle2), sent+delivered+read count
- "Falhas" - red icon (XCircle), failed count

Each card: `rounded-xl border border-zinc-800/60 bg-zinc-900/60 p-4` with icon in colored bg pill top-right, value in text-2xl font-bold text-white, label in text-xs text-zinc-500 uppercase.

**Section 3: 7-day bar chart** (rounded-xl border border-zinc-800/60 bg-zinc-900/60 overflow-hidden):
- Header: `px-5 py-4 border-b border-zinc-800/60` with title "Ultimos 7 dias" and subtitle "Disparos por dia"
- Chart area: `px-5 py-6` with flex container, 7 bars
- Each bar: flex-1, vertical column. Bar itself uses `div` with `bg-indigo-500` and `style={{ height: \`${percentage}%\` }}` where percentage = (count / maxCount) * 100, min height 4px if count > 0
- Bar container height: `h-40`
- Below each bar: day label (text-[10px] text-zinc-500) and count (text-xs text-zinc-400)
- If all days are 0, show "Nenhum disparo nos ultimos 7 dias" centered message

**Section 4: Two-column grid** (grid-cols-1 lg:grid-cols-2 gap-4):

Left: **Produtos por Marketplace** card:
- Header: "Produtos por Marketplace"
- For each marketplace (Amazon, ML, Shopee, AliExpress), show:
  - Marketplace name + count on same line
  - Horizontal bar below: `h-2 rounded-full bg-zinc-800` container, inner `bg-{color}` div with width percentage relative to max marketplace count
  - Colors: amazon=orange-500, mercadolivre=yellow-500, shopee=red-500, aliexpress=rose-500
- If no products: "Nenhum produto coletado ainda"

Right: **WhatsApp Instance** card:
- Header: "Instancia WhatsApp"
- If connected: green dot + "Conectado" + instance status
- If not connected: red dot + "Desconectado" + link to /canais
- Use MessageCircle icon

**Section 5: Keep the "Proximos passos" section** for users who haven't completed setup (hasMarketplace, hasWhatsApp, hasGroup). Show it AFTER the charts, not instead of them.

**IMPORTANT:**
- Do NOT import recharts. Use pure CSS for bar heights via inline style `width` or `height` percentages.
- Use lucide-react icons: Package, Send, Clock, CheckCircle2, XCircle, MessageCircle, BarChart3
- Keep the `getGreeting()` function
- All text in Portuguese (PT-BR)
- Marketplace labels: { amazon: 'Amazon BR', mercadolivre: 'Mercado Livre', shopee: 'Shopee', aliexpress: 'AliExpress' }
  </action>
  <verify>
    <automated>grep -c "dispatched_date" src/app/(dashboard)/page.tsx && grep "recharts" src/app/(dashboard)/page.tsx; echo "exit: $?"</automated>
  </verify>
  <acceptance_criteria>
    - page.tsx queries dispatch_logs with gte dispatched_date for 7-day range
    - page.tsx does NOT import from "recharts"
    - page.tsx contains "Ultimos 7 dias" text for chart header
    - page.tsx contains bar chart with inline style height percentages (grep for "height:" or "style={{")
    - page.tsx renders 5 stat cards: "Total Produtos", "Enviados hoje", "Pendentes", "Sucesso", "Falhas"
    - page.tsx queries whatsapp_instances table for connection status
    - page.tsx contains "Produtos por Marketplace" section
    - page.tsx contains "Instancia WhatsApp" section
    - page.tsx imports Package, Send or SendHorizonal, Clock, CheckCircle2, XCircle from lucide-react
    - page.tsx is a server component (no 'use client' directive)
    - `npx tsc --noEmit` passes
  </acceptance_criteria>
  <done>Dashboard displays 7-day bar chart with CSS bars, 5 stat cards, marketplace product distribution bars, and WhatsApp status card. No recharts dependency.</done>
</task>

</tasks>

<verification>
- `grep "recharts" src/app/(dashboard)/page.tsx` returns no match
- `grep "Ultimos 7 dias" src/app/(dashboard)/page.tsx` returns match
- `grep "whatsapp_instances" src/app/(dashboard)/page.tsx` returns match
- `grep "Produtos por Marketplace" src/app/(dashboard)/page.tsx` returns match
- `npx tsc --noEmit` passes
</verification>

<success_criteria>
- Dashboard renders 7-day CSS bar chart from dispatch_logs data
- Dashboard shows 5 stats cards (Total, Enviados, Pendentes, Sucesso, Falhas)
- Dashboard shows products per marketplace with colored progress bars
- Dashboard shows WhatsApp connection status
- No recharts or external chart library added
</success_criteria>

<output>
After completion, create `.planning/phases/03-automacao-fila/03-02-SUMMARY.md`
</output>
